// kelvin is always 293

const kelvin = 293;

// celcius is always 293-273

var celcius = (kelvin - 273);

// fahrenheit fron celcius conversion, rounded down

var fahrenheit = celcius * ((9/5) + 32));

var TEMPERATURE = fahrenheit;

// console.log(`The temperature is ${TEMPERATURE} degrees Fahrenheit`);

